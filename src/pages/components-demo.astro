---
import '../styles/global.css';
import CodeBlock from '../components/CodeBlock.astro';
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content={Astro.generator} />
    <title>dstack Component Demo - CodeBlock</title>

    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" integrity="sha512-DTOQO9RWCH3ppGqcWaEA1BIZOC6xxalwEsw9c2QQeAIftl+Vegovlnee1c9QX4TctnWMn13TZye+giMm8e2LwA==" crossorigin="anonymous" referrerpolicy="no-referrer" />
  </head>
  <body class="bg-bg-space text-text-primary">
    <div class="min-h-screen py-12 px-4">
      <div class="max-w-4xl mx-auto">
        <!-- Header -->
        <div class="text-center mb-12">
          <h1 class="text-4xl font-bold mb-4">
            <span class="text-lime-green">CodeBlock</span> <span class="text-cyber-blue">Component</span>
          </h1>
          <p class="text-text-secondary text-lg">Phase 0.3 - Step 1</p>
        </div>

        <!-- CodeBlock Demo -->
        <section class="mb-12">
          <h2 class="text-2xl font-bold text-text-primary mb-4">Basic Code Block</h2>
          <CodeBlock
            code={`npm install`}
            language="bash"
          />
        </section>

        <section class="mb-12">
          <h2 class="text-2xl font-bold text-text-primary mb-4">With Title</h2>
          <CodeBlock
            code={`# Install dependencies
sudo apt update && sudo apt upgrade -y
sudo apt install -y build-essential git curl

# Verify installation
which gcc
gcc --version`}
            language="bash"
            title="System Setup Commands"
          />
        </section>

        <section class="mb-12">
          <h2 class="text-2xl font-bold text-text-primary mb-4">Multiple Languages</h2>

          <CodeBlock
            code={`function greet(name) {
  console.log(\`Hello, \${name}!\`);
}

greet('dstack');`}
            language="javascript"
            title="JavaScript Example"
          />

          <CodeBlock
            code={`def greet(name):
    print(f"Hello, {name}!")

greet("dstack")`}
            language="python"
            title="Python Example"
          />
        </section>

        <section class="mb-12 p-6 bg-bg-card border border-border-default rounded-lg">
          <h3 class="text-xl font-bold mb-4 text-lime-green">Testing Instructions</h3>
          <ul class="list-disc list-inside text-text-secondary space-y-2">
            <li>Hover over any code block - copy button should appear</li>
            <li>Click copy button - text should change to "Copied!"</li>
            <li>Paste clipboard - code should match exactly</li>
            <li>Test keyboard navigation - Tab to button, Enter to copy</li>
            <li>Test on mobile - touch targets should be adequate</li>
          </ul>
        </section>
      </div>
    </div>
  </body>
</html>

# Ansible
*.retry

# Actual inventory files (keep only .example files in git)
inventory/hosts.yml
inventory/*.yml
!inventory/*.example.yml

# Actual variables files (keep only .example files in git)
group_vars/all.yml
group_vars/*.yml
!group_vars/*.example.yml

# Quick-start variables (keep only .example files in git)
vars/quick-start.yml
vars/local-secrets.yml
vars/contract-addresses.yml
vars/*.yml
!vars/*.example.yml

# Secrets and sensitive data
*.pem
*.key
*.crt
*.vault

# Ansible Galaxy
roles/*/
!roles/.gitkeep
